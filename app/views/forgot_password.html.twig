{% extends 'layouts/account.html.twig' %}

{% set title = 'forgot_password'|trans %}

{% block content %}
    <div class="panel panel-primary">
        <div class="panel-body">
            {% form_theme form 'bootstrap_3_layout.html.twig' %}
            {{ form(form, {attr: {class: 'form-panel'}}) }}
        </div>
        <div class="panel-footer">
            <p class="text-right no-margin-bottom">{{ 'forgot_password_help'|trans({'%link%': path('login')})|raw }}</p>
        </div>
    </div>
{% endblock %}
